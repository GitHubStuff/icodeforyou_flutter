name: Windows

on:
  pull_request:
    paths:
      - "plugins/**/windows/**"

jobs:
  windows:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build Windows
        run: |
          cmake -S . -B build
          cmake --build build --config Release

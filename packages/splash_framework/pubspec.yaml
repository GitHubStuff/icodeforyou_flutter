name: splash_framework
description: "Manages the display of a splash screen will performing background/startup tasks."
publish_to: none
homepage:
resolution: workspace
version: 1.0.0

environment:
  sdk: ^3.12.0

dependencies:
  custom_widgets: ^1.0.0
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  icodeforyou_lints: ^1.0.0

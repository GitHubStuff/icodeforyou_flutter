name: position_popover
description: Service-locator integration providing a TagsCubit backed by the SinceWhen service. STUBBED — see ice_chips_descriptor.dart for migration notes.
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  extensions: ^1.0.3
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.2.0

flutter:
  uses-material-design: true

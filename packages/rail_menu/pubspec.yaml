# packages/rail_menu/pubspec.yaml

name: rail_menu
description: "A new Flutter package project."
version: 0.0.1
resolution: workspace
publish_to: none

environment:
  sdk: ">=3.10.0 <4.0.0"
  
dependencies:
  animated_widgets:
    path: ../animated_widgets
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  
dev_dependencies:
  bloc_test: ^10.0.0
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  very_good_analysis: ^10.2.0

flutter:
  uses-material-design: true
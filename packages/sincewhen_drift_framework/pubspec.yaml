# packages/sincewhen_drift_framework/pubspec.yaml
name: sincewhen_drift_framework
description: "SinceWhen using Drift database front-end"
publish_to: none
homepage:
resolution: workspace
version: 1.0.0

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  drift: ^2.34.0
  drift_flutter: ^0.3.1
  extensions: ^1.0.0   # color.toInt()
  path_provider: ^2.1.6
  random_color_generator: ^1.0.0

dev_dependencies:
  build_runner: ^2.15.1    # dart run build_runner build
  drift_dev: ^2.34.0
  flutter_test:
    sdk: flutter
  icodeforyou_lints: ^1.0.0

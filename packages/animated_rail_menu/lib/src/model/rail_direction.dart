// lib/src/model/rail_direction.dart

/// The axis along which the [AnimatedRailMenu] bar is rendered.
enum RailDirection {
  /// Renders the menu bar along the bottom edge (horizontal).
  horizontal,

  /// Renders the menu bar along the left edge (vertical).
  vertical,
}

name: color_grid
description: "Displays a grid of color options for use in the sincewhen_framework"
version: 1.0.0
resolution: workspace
publish_to: "none"

environment:
  sdk: ">=3.12.0 <4.0.0"
  
dependencies:
  extensions: ^1.0.4
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.3.0
  
flutter:
  uses-material-design: true
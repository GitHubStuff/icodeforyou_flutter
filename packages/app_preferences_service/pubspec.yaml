name: app_preferences_service
description: "Service-locator handle and descriptor for app_preferences."
version: 1.0.0
resolution: workspace
publish_to: "none"

environment:
  sdk: ^3.12.0

dependencies:
  app_preferences: ^1.0.0
  service_locator: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  icodeforyou_lints: ^1.0.0
  shared_preferences_platform_interface: ^2.4.0

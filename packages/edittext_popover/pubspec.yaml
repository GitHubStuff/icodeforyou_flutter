# pubspec.yaml
name: edittext_popover
description: A cross-platform text editor overlay widget with line/character counting and theme support.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.9.0"
  flutter: ">=3.35.5"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1

dev_dependencies:
  bloc_test: ^10.0.0
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  very_good_analysis: ^10.0.0

flutter:
  uses-material-design: true
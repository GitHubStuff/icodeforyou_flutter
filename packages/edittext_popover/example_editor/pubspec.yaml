# pubspec.yaml
name: example_editor
description: Smoke test for edittext_popover package.
version: 0.1.1
publish_to: none

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  edittext_popover:
    path: ../../edittext_popover
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.2.0

flutter:
  uses-material-design: true
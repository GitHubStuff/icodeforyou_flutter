// edittext_popover/lib/src/constants.dart

/// Opacity of the modal barrier behind the popover.
const double kBarrierOpacity = 0.4;

/// Standard height for action buttons.
const double kButtonHeight = 56;

/// Horizontal padding inside action buttons.
const double kButtonHorizontalPadding = 24;

/// General padding around buttons.
const double kButtonPadding = 16;

/// Corner radius for action buttons.
const double kButtonRadius = 24;

/// Vertical spacing between buttons.
const double kButtonSpacing = 16;

/// Vertical padding inside action buttons.
const double kButtonVerticalPadding = 12;

/// Elevation of card surfaces.
const double kCardElevation = 8;

/// Internal padding within cards.
const double kCardPadding = 16;

/// Corner radius for cards.
const double kCardRadius = 16;

/// Default font size for button labels.
const double kDefaultButtonTextSize = 24;

/// Default font size for body text.
const double kDefaultTextSize = 18;

/// Gap between editor elements.
const double kEditorGap = 8;

/// Height of the popover editor panel.
const double kEditorHeight = 350;

/// Outer margin around the editor panel.
const double kEditorMargin = 16;

/// Width of the popover editor panel.
const double kEditorWidth = 400;

/// Fraction of screen height used by the full-screen text field.
const double kFullScreenTextFieldRatio = 0.7;

/// Spacing below the stats row.
const double kSpacingAfterStats = 8;

/// Spacing below the text field.
const double kSpacingAfterTextField = 16;

/// Font size for the character/word stats label.
const double kStatsFontSize = 14;

/// Height of the stats row.
const double kStatsHeight = 32;

/// Right padding on the stats row.
const double kStatsRightPadding = 8;

/// Internal content padding for the text field.
const double kTextFieldContentPadding = 12;

/// Border width when the text field is focused.
const double kTextFieldFocusedBorderWidth = 2;

/// Corner radius for the text field.
const double kTextFieldRadius = 12;

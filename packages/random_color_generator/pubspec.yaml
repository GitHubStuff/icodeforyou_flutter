name: random_color_generator
description: "Generates random colors and helper methods"
version: 1.0.1
resolution: workspace
publish_to: none

environment:
  sdk: ^3.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  icodeforyou_lints: ^1.0.0

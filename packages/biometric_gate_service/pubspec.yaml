# packages/biometric_gate_service/pubspec.yaml
name: biometric_gate_service
description: Backend-agnostic interface for biometric-gated secure storage.
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  fpdart: ^1.2.0
  meta: ^1.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.2.0
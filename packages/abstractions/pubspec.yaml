# pubspec.yaml
# Flutter 3.32.8 / Dart ">3.10.0"
# Minimal dependencies for abstractions package

name: abstractions
description: "Package to handle all abstract classes"
version: 0.0.1
resolution: workspace

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.2.0

flutter:
  uses-material-design: true
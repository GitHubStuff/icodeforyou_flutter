name: projector
description: "Designed to animate .apng {Animated PNG} file (apng is a collection of images in a grid)."
version: 1.0.0
publish_to: none
homepage:
resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  icodeforyou_lints: ^1.0.0

flutter:
  uses-material-design: true

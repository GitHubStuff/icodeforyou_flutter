name: remind_me
description: "Package to handle scheduling local notifications"
version: 1.0.0
publish_to: none
homepage:
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  flutter_local_notifications: ^21.0.0
  flutter_timezone: ^5.0.2
  permission_handler: ^12.0.3
  timezone: ^0.11.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  icodeforyou_lints: ^1.0.0

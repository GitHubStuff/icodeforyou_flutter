# packages/dependency_resolver/pubspec.yaml
name: dependency_resolver
description: "GET_IT wrapper and mock for service provider"
publish_to: none
resolution: workspace
version: 1.0.0

environment:
  sdk: ^3.12.0

dependencies:
  get_it: ^9.2.1

dev_dependencies:
  icodeforyou_lints: ^1.0.0
  mocktail: ^1.0.5
  test: ^1.25.0
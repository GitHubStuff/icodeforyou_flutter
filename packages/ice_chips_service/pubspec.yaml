# packages/ice_chips_service/pubspec.yaml
name: ice_chips_service
description: Service-locator integration providing a TagsCubit backed by the SinceWhen service. STUBBED — see ice_chips_descriptor.dart for migration notes.
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  ice_chips: ^1.0.0
  service_locator: ^1.0.0
  since_when_service: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.2.0

flutter:
  uses-material-design: true

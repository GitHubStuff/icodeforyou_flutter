name: service_locator
description: "Manages Services and their dependencies"
version: 1.0.0
publish_to: none
resolution: workspace
homepage:

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  extensions: ^1.0.0
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  gap: ^3.0.1
  get_it: ^9.2.1
  my_logger: ^1.0.0

 

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.2.0

flutter:
  uses-material-design: true

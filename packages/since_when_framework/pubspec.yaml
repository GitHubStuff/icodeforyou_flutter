name: since_when_framework
description: Generic SQLite lifecycle scaffold plus since_when table setup/reset.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.5.0
  flutter: ">=3.24.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.5
  flutter_bloc: ^9.1.1
  fpdart: ^1.1.0
  path: ^1.9.0
  path_provider: ^2.1.4
  sqflite: ^2.4.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.3.0

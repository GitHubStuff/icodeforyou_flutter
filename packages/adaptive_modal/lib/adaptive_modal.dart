// lib/adaptive_modal.dart
// ---------------------------------------------------------------------------
// adaptive_modal.dart — public barrel file.
//
// Only [AdaptiveModalController] and [AdaptiveModalConfig] are exported.
// All implementation files are prefixed with _ and remain private to the
// package.
// ---------------------------------------------------------------------------


export 'src/_controller.dart' show AdaptiveModalController;
export 'src/types.dart' show AdaptiveModalConfig;

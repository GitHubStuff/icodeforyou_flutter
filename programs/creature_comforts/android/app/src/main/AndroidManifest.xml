<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- ADDED: required by flutter_local_notifications -->
    <!-- Runtime permission on Android 13+ to display notifications at all -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Runtime permission on Android 14+ to schedule exact-time alarms (±1 min target) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <!-- Lets the plugin re-register pending notifications after device reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@drawable/empty_splash_icon">
        <!-- empty_splash_icon lets the app start with a black screen, and no icon {use app splash screen}-->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"
                />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- ADDED: required by flutter_local_notifications -->
        <!-- Receives the OS callback when a scheduled alarm fires, then posts the notification -->
        <receiver android:exported="false" android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
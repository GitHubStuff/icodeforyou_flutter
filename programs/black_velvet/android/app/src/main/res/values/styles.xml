<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme for the Android 12+ system splash / starting window.
         KNOWN LIMITATION: the splash window is OS-owned; the status bar
         cannot be hidden during it by any app mechanism. -->
    <!-- CHANGED: parents Theme.Light.NoTitleBar -> Theme.Black.NoTitleBar.
         With no explicit icon-appearance flags, Android derives status icon
         color from the theme's lightness. Light parent = dark icons =
         invisible on a black app (the "only the battery's green fill shows"
         bug). Black parent = white icons in both day and night modes, which
         is also why deleting values-night regressed this: the night file's
         Black parent was what dark-mode devices were relying on. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowSplashScreenBackground">@android:color/black</item>
        <item name="android:windowSplashScreenAnimatedIcon">@android:color/transparent</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>